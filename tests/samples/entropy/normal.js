// Normal JavaScript file with low-entropy content
const greeting = "Hello World";
const name = "MUAD'DIB Scanner";

function add(a, b) {
  return a + b;
}

function multiply(x, y) {
  return x * y;
}

const config = {
  port: 3000,
  host: "localhost",
  debug: false
};

module.exports = { greeting, name, add, multiply, config };
